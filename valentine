index.html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Puntungri 💖 My Valentine</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ffd6e8, #fff0f6);
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: "Comic Sans MS", "Poppins", cursive;
      overflow: hidden;
    }

    .card {
      background: white;
      padding: 34px;
      border-radius: 30px;
      text-align: center;
      box-shadow: 0 25px 50px rgba(0,0,0,0.15);
      max-width: 340px;
      z-index: 2;
      animation: pop 0.8s ease;
    }

    @keyframes pop {
      0% { transform: scale(0.6); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }

    .name {
      font-size: 22px;
      color: #ff7aa8;
      margin-bottom: 8px;
    }

    h1 {
      color: #ff4d88;
      font-size: 26px;
    }

    p {
      color: #555;
      font-size: 16px;
      margin-top: 10px;
    }

    .buttons {
      margin-top: 24px;
      display: flex;
      justify-content: center;
      gap: 16px;
    }

    button {
      border: none;
      padding: 12px 26px;
      font-size: 16px;
      border-radius: 30px;
      cursor: pointer;
      transition: 0.3s ease;
    }

    #yesBtn {
      background: #ff4d88;
      color: white;
    }

    #yesBtn:hover {
      transform: scale(1.1);
      box-shadow: 0 0 18px #ff99bb;
    }

    #noBtn {
      background: #ddd;
      color: #666;
    }

    .message {
      display: none;
      margin-top: 22px;
      font-size: 18px;
      color: #ff4d88;
    }

    /* Teddy */
    .teddy {
      display: none;
      font-size: 90px;
      margin-top: 15px;
      animation: teddyPop 1s ease forwards;
    }

    @keyframes teddyPop {
      0% { transform: translateY(50px) scale(0.5); opacity: 0; }
      60% { transform: translateY(-10px) scale(1.1); }
      100% { transform: translateY(0) scale(1); opacity: 1; }
    }

    /* Floating hearts */
    .heart {
      position: absolute;
      bottom: -20px;
      font-size: 20px;
      animation: float 6s linear infinite;
      opacity: 0.8;
    }

    @keyframes float {
      0% { transform: translateY(0); opacity: 1; }
      100% { transform: translateY(-110vh); opacity: 0; }
    }

    iframe { display: none; }
  </style>
</head>

<body>

  <!-- Ed Sheeran - Perfect -->
  <iframe
    src="https://www.youtube.com/embed/2Vv-BfVoq4g?autoplay=1&loop=1&playlist=2Vv-BfVoq4g"
    allow="autoplay">
  </iframe>

  <div class="card">
    <div class="name">Hey Puntungri 🥰</div>
    <h1>Will you be my Valentine? 💘</h1>

    <p>
      You make my heart feel cozy like lemon tea 🍋☕  
      and soft like my favorite book 📖
    </p>

    <div class="buttons">
      <button id="yesBtn">Yes 💖</button>
      <button id="noBtn">No 😢</button>
    </div>

    <div class="teddy" id="teddy">🧸💗</div>

    <div class="message" id="message">
      Puntungri, let’s go on a <b>lemon tea date</b>  
      with a <b>boutique of books</b> 📚🍋💐
    </div>
  </div>

  <script>
    const noBtn = document.getElementById("noBtn");
    const yesBtn = document.getElementById("yesBtn");
    const message = document.getElementById("message");
    const teddy = document.getElementById("teddy");

    noBtn.addEventListener("click", () => {
      noBtn.style.display = "none";
    });

    yesBtn.addEventListener("click", () => {
      yesBtn.style.display = "none";
      noBtn.style.display = "none";
      teddy.style.display = "block";
      message.style.display = "block";
      createHearts();
    });

    function createHearts() {
      setInterval(() => {
        const heart = document.createElement("div");
        heart.classList.add("heart");
        heart.innerHTML = "💖";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.fontSize = Math.random() * 20 + 15 + "px";
        document.body.appendChild(heart);

        setTimeout(() => heart.remove(), 6000);
      }, 250);
    }
  </script>

</body>
</html>
